using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Interaction;

public class BCell : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Player bCellPlayer = new Player(10, 3);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
